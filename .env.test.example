DATABASE_URL=postgresql://user:password@host/test_db_branch?sslmode=require
NODE_ENV=test
PORT=4001
JWT_SECRET=replace-with-a-long-random-test-only-secret
JWT_EXPIRES_IN=1h
PASSWORD_RESET_EXPIRES_MINUTES=15
FRONTEND_URL=http://localhost:5173
# SMTP_USER / SMTP_PASS intentionally omitted: emails no-op (logged, not sent) in the test branch.
